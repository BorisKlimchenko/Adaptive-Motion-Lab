# --- Core Infrastructure ---
# Мы разрешаем любую версию выше 2.1.0, чтобы принять ту (2.5.1), 
# которую мы принудительно поставили в Лаунчере.
torch>=2.1.0
torchvision>=0.16.0

# --- Generative Stack ---
diffusers>=0.26.0
transformers>=4.38.0
accelerate>=0.27.0
safetensors>=0.4.2

# --- Optimization ---
# xFormers уже установлен в Лаунчере. 
# Эта строка нужна только для локальной разработки на Linux.
xformers>=0.0.28; sys_platform == 'linux'

# --- Utils ---
pillow>=10.0.0
numpy>=1.24.0